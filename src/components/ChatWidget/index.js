import React from "react"
import { Card } from "react-bootstrap"
import { Widget } from "./ChatWidget.styles"
const ChatWidget = () => (
	<Widget>
		<h1>Vedant</h1>
		<p>Join Chat</p>
	</Widget>
)

export default ChatWidget
